---
- hosts: localhost
  connection: local
  pre_tasks:

    - name: Detect Host User
      ansible.builtin.import_tasks: '{{ pre_tasks_dir }}/whoami.yml'
      tags:
        - always

  roles:
    - zsh
