---
# Default roles to run if no roles are specified
default_roles:
  # Standalone roles
  - system
  - fonts
  - lua
  - fzf
  - nvm
  - node
  - ssh
  - tmux
  - docker

  # Dependant roles
  - zsh
  - neovim
